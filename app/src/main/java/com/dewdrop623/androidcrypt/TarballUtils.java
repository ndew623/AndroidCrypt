package com.dewdrop623.androidcrypt;

import android.content.Context;
import android.support.v4.provider.DocumentFile;

import java.io.FileOutputStream;
import java.util.ArrayList;

/*TarballUtils tarballs and untarballs files*/
public class TarballUtils {
    /**
     * Creates a tar.
     * @param context
     *          For showing errors to the user.
     * @param fileList
     *          List of files to be put into tar.
     */
    public static void tarFiles(Context context, ArrayList<DocumentFile> fileList, DocumentFile outputDirectory, String outputFileName) {
        //outputDirectory.createFile(outputFileName, )
        //FileOutputStream fileDestination =
    }
}
